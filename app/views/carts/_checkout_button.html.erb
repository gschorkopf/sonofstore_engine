
<%if false %>
<% if current_user %>
  <%= form_tag user_orders_path(current_user) do %>
    <script src="https://checkout.stripe.com/v2/checkout.js" class="stripe-button"
            data-key="pk_test_osSOH0LyEEACshdRsLjyvGSz"
            data-image="/assets/marcy_stripe.png"
            data-name="<%= t('store.name') %>"
            data-description="<%= t('purchase') %>"
            data-label="<%= t('checkout') %>"
            data-amount="<%= price * 100 %>">
    </script>
  <% end %>
<% else %>
  <%= button_to t('checkout'), login_path, class: 'btn btn-custom-lighten', method: :get %>
<% end %>
  <%= button_to "Checkout", guest_checkout_path, class: 'btn btn-custom-lighten', method: :get %>
<%end%>

<% if current_user && current_user.shipping_address && current_user.billing_address && current_user.credit_card %>
  <%= button_to ("checkout"), new_user_order_path(current_user), method: :get %>
<% elsif current_user %>
  <%= button_to t("checkout"), new_user_shipping_addresses_path(current_user), method: :get %>
<% else %>
  <%= button_to "Checkout", login_path, class: 'btn btn-custom-lighten', method: :get %>
<% end %>


