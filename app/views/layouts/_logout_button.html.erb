<% if session[:cart] && !session[:cart].empty? %>
  <%= link_to 'navigation.logout', session_path(current_user), method: :delete, confirm: 'cart.confirmlogout' %>
<% else %>
  <%= link_to 'navigation.logout', session_path(current_user), method: :delete %>
<% end %>
