<%= simple_form_for [:admin, @category] do |f| %>
  <%= f.input :title, input_html: { placeholder: 'Dark Matter' } %>
  <%= f.hidden_field :store_id, value: @store.id %>
  <%= f.button :submit, 'Submit', class:'btn btn-custom-lighten' %>
<% end %>
