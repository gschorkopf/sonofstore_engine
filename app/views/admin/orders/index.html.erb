<div class="admin-order-index">
  <%= render partial: 'shared/admin_navigation',
             locals: { active: current_page?(store_admin_orders_path(current_store)) ? :dashboard : :search } %>

  <div class="span8">
    <h1><% if current_page?(store_admin_orders_path(current_store)) %><%= 'admin.dashboard' %><% else %>Search<%end%></h1>
    <hr>

    <% if @count > 0 %>
      <%= render partial: 'admin_dashboard_nav', locals: {active_tab: @active_tab} %>
        <br>
        <table class="table">
          <thead>
          <th>#</th>
          <th><%= 'Created At' %></th>
          <th><%= 'Status' %></th>
          <th><%= 'Modify' %></th>
          </thead>
          <tbody>
          <%= render @orders %>
          </tbody>
        </table>
    <% else %>
        <%= "No orders" %>
    <% end %>
  </div>
</div>
