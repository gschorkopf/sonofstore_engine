<div class="row content-box">
  <div class="prod_detail_img span6">
    <P><%= image_tag generate_image_url(500, @product.id), class: "img-rounded" %></P>
    <!-- <p><%#= image_tag @product.image.url(:large) %></p> -->
  </div>

  <div class="prod_detail_text span5">
    <h2><% 'Title' %><%= @product.title %></h2>
    <p><% 'Description' %><%= @product.description %></p>

    <div id="product-categories">
      <b><%= 'Category' %>:</b>
      <% @product.categories.each do |category| %>
         <span class='label'><%= link_to category.title, store_category_path(@product.store.path, category), method: :get %></span>

      <% end %>
    </div>

    <br>
      <p><%= 'Price' %>: <%= number_to_currency(@product.price) %></p>
    <% if @product.status == 'active' %>
      <%= render partial: 'add_to_cart_button', locals: {product: @product} %>
    <% else %>
      <p> Product is retired! </p>
    <% end %>
  </div>
</div>
<div class="row content-box">
      <div id="product-average-rating" class="span6">
        <table class="product-rating">
          <thead>
            <tr>
              <th>Criteria</th>
              <th>Average Rating</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td class="criteria">Packaging</td>
              <td>5</td>
            </tr>
            <tr>
              <td class="criteria">Durability</td>
              <td>4</td>
            </tr>
            <tr>
              <td class="criteria">Value</td>
              <td>1</td>
            </tr>
            <tr>
              <td class="criteria">Description Accuracy</td>
              <td>1</td>
            </tr>
          </tbody>
        </table>
        <a class="product-add-rating" href="product_rating.html">Reivew this!</a> </div> <!-- product-average-rating -->
      <div id="product-reviews" class="span6">
        <div class="review">

          <div class="reivew-meta-data">
            Reviewed By: <span class="reviewer">Sir CatNap</span> on <span clas="review-date">January 27, 2012</span>
          </div>
          <div class="review-comment">
            Everything was perfect about this Cute Kitten EXCEPT that it wasn't a kitten! It was an E-kitten! I was very disappointed. Don't think I'll buy this again!
          </div>

          <table class="product-rating">
            <thead>
              <tr>
                <th>Criteria</th>
                <th>Average Rating</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td class="criteria">Packaging</td>
                <td>4</td>
              </tr>
              <tr>
                <td class="criteria">Durability</td>
                <td>5</td>
              </tr>
              <tr>
                <td class="criteria">Value</td>
                <td>1</td>
              </tr>
              <tr>
                <td class="criteria">Description Accuracy</td>
                <td>1</td>
              </tr>
            </tbody>
          </table>
        </div>
      </div> <!-- product-reviews -->
    </div>
