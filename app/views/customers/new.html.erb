<%= simple_form_for @customer do |f| %>
    <%= f.input :full_name, input_html: { placeholder: 'placeholder.user.fullname' } %>
    <%= f.input :email, input_html: { placeholder: 'placeholder.user.email' } %>
    <%= f.button :submit, 'submit' %>
<% end %>
